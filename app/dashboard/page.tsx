// app/dashboard/page.tsx
// Halaman awal — due today, mastered count per level, overview deck.
// TODO(frontend): ganti dengan hasil review Stitch (lihat stitch/02-dashboard.md)
// TODO(srs-engine-agent): implementasikan query due today + mastered count per level

export default function DashboardPage() {
  return (
    <main style={{ padding: "2rem", fontFamily: "sans-serif" }}>
      <h1>Dashboard</h1>
      <p>TODO: due today, mastered count per level HSK, overview deck.</p>
    </main>
  );
}
